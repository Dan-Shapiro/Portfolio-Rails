<div id="post_show_content" class="skinny_wrapper wrapper_padding">
	<header>
		<p class="date"><%= @climb.created_at.strftime("%A, %B %d") %></p>
		<h1><%= @climb.title %> <span class="grade"><%= @climb.grade %></span></h1>
		<hr>
	</header>

	<div id="content">
		<%= @climb.content %>
	</div>
